# Everything below is OPTIONAL. The app works fully with zero keys set,
# using only free scraping + no-key public APIs (Whois, Wikipedia, GitHub).
# If you later create free accounts, drop the keys in and the corresponding
# source turns on automatically next request. No code changes needed.

# Free forever, 50 credits/month, no card required: https://hunter.io/users/sign_up
HUNTER_API_KEY=

# Free tier available: https://www.apollo.io/sign-up
APOLLO_API_KEY=
